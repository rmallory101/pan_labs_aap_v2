---
ansible-navigator:
  ansible:
    inventory:
      entries:
      - inventory
  execution-environment:
    container-engine: podman
    enabled: true
    image: localhost/pan-lab-ee-11:latest
    pull:
      policy: missing
  mode: stdout
  playbook-artifact:
    enable: false
